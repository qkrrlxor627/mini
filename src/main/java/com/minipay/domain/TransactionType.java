package com.minipay.domain;

public enum TransactionType {
    CHARGE, PAYMENT
}

/*
 * [주석 목적]
 * 1. 내가 작성한 주석
 *    - line 3: "transactionType 충전, 결제"
 *    - line 4: "class가 아닌 enum 자율성을 위함"
 *    - line 5: "순서 상관없고, 관례상 상수라 대문자, enum인데 따로 더 데이터 정의없어서 ;안 써도 됨."
 *
 * 2. 오답 수정 및 정리
 *    - line 3 (불필요 / WHAT 주석):
 *        클래스명 TransactionType + 값 CHARGE, PAYMENT가 이미 같은 정보를 줌.
 *        "이름이 설명하는 걸 주석으로 또 쓰지 마라" 룰에 위배 → 삭제.
 *
 *    - line 4 (의미 거꾸로 적힘):
 *        "자율성"은 enum의 특징이 아님. 오히려 enum은 선택지를 강제로 제한하는 도구.
 *        정확한 이유:
 *          (a) 타입 안전성 — 오타/잘못된 값 컴파일 단계에서 차단
 *          (b) 유한한 선택지 명시 — "충전/결제 둘뿐"이 코드에 박힘
 *          (c) switch 망라성 / 싱글톤 보장 / JPA 매핑 깔끔
 *
 *    - line 5 (내용 OK, 위치 부적절):
 *        세 정보 다 정답 — STRING 매핑이라 순서 무관, 상수=대문자 관례, 멤버 없으면 ';' 생략 가능.
 *        하지만 "왜 이 코드가 이렇게 생겼는지"가 아니라 자바 문법 학습 메모.
 *        → 본 코드 주석이 아니라 학습 노트(progress.md)로 가는 게 맞음.
 *
 * 3. 좋은 주석 룰 (CLAUDE.md / 시스템 룰 요약)
 *    - WHAT 금지: 이름이 이미 설명함
 *    - WHY만 적되, 명백하면 생략
 *    - 코드만 봐선 알 수 없는 "숨은 제약/이유"만 남길 것
 *        예) // 결제 정산 보고서가 enum 순서로 정렬되므로 바꾸면 리포트 깨짐
 */
